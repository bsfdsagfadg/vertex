# Runtime config (contains secrets)
config/config.json
config/api_keys.txt
config/nodes.json
config/node_health.json

# SQLite database and WAL files
config/data.db
config/data.db-shm
config/data.db-wal

# Binaries
vertex-proxy
vertex-proxy-*
*.exe
*~

# Release packaging output
dist/

# IDE / OS
.vscode/
.idea/
.kilo/
*.swp
.DS_Store

# Test artifacts
test-generated-*.png
*.code-workspace

# Runtime state (auto-generated, never commit)
config/state/

# 运行时目录：管理员后台上传的自定义背景图（由程序自动创建，勿提交）
/assets/

# Logs
logs/
